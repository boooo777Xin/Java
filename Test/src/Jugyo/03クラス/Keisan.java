class Keisan {
    public static void main(String[] args) {
        Calculation cal = new Calculation();

        // 足し算
        cal.plus(10);
        cal.display();

        // 足し算
        cal.plus(40);
        cal.display();

        // 足し算
        cal.plus(50);
        cal.display();

        // 足し算
        cal.plus(20);
        cal.display();
    }
}